<%- include('../partials/head', { title: '本を編集' }) %>
<div class="page-top"><a class="back" href="/books/<%= book._id %>">← 詳細に戻る</a><p class="eyebrow">EDIT BOOK</p><h1>本の記録を<br><em>整える。</em></h1><p>読書が進んだら、状態やメモを更新しましょう。</p></div>
<form class="book-form" action="/books/<%= book._id %>?_method=PUT" method="post">
    <%- include('../partials/form', { book, statuses, genres, errors }) %>
    <div class="form-actions"><a class="text-link" href="/books/<%= book._id %>">キャンセル</a><button class="button" type="submit">変更を保存する</button></div>
</form>
<%- include('../partials/foot') %>
